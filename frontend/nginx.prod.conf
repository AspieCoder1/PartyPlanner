worker_processes 1;
events { worker_connections 1024; }
http {
    include    mime.types;
    gzip on;
    gzip_types *;

    server {
        listen 80;

        location / {
            try_files $uri /index.html;
        }

        rewrite /socket.io /chat-service/socket/ redirect;
    }
}